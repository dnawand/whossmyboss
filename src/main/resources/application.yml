micronaut:
  application:
    name: whosmyboss

datasources:
  default:
    url: jdbc:h2:mem:hrdatabase;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: ""
    driverClassName: org.h2.Driver

flyway:
  datasources:
    default:
      enabled: true
